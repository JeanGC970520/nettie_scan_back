[project]
name = "nettie-scan-back"
version = "0.1.0"
description = "NettieScan is an application to scann handwritten into a image"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.40.58",
    "fastapi[standard]>=0.119.1",
    "pytest>=8.4.2",
]
